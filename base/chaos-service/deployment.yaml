apiVersion: apps/v1
kind: Deployment
metadata:
  name: chaos-service
  namespace: app
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  labels:
    app: chaos-service
    app.kubernetes.io/name: chaos-service
    app.kubernetes.io/part-of: major-project
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chaos-service
  template:
    metadata:
      labels:
        app: chaos-service
        app.kubernetes.io/name: chaos-service
        app.kubernetes.io/part-of: major-project
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000

      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchLabels:
                  app: chaos-service
              topologyKey: kubernetes.io/hostname

      containers:
        - name: chaos-service
          image: major-project-chaos-service:latest
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: app-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
              ephemeral-storage: 100Mi
            limits:
              cpu: 500m
              memory: 256Mi
              ephemeral-storage: 200Mi
          securityContext:
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3

      volumes:
        - name: tmp
          emptyDir: {}
