apiVersion: v1
kind: Service
metadata:
  name: worker-service
  namespace: app
  labels:
    app: worker-service
    app.kubernetes.io/name: worker-service
    app.kubernetes.io/part-of: major-project
spec:
  selector:
    app: worker-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
