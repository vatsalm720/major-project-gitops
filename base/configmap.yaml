apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: app
  labels:
    app.kubernetes.io/part-of: major-project
data:
  REDIS_HOST: redis
  REDIS_PORT: "6379"
  REDIS_DB: "0"
  TASK_QUEUE_KEY: tasks:queue
  POLL_TIMEOUT_SECONDS: "5"
